import styled from '../../../styled-components';
import { getHeadingTypography } from '../../../theme/helpers';

export const MainContactInfo = styled.span`
    ${getHeadingTypography}
`;

export default MainContactInfo;
